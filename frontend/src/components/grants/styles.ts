/** Shared inline-style constants for the Manage Privileges wizard (kept component-free for fast-refresh). */
import type { CSSProperties } from "react";
import { C } from "../../utils/colors";

export const fieldLabel: CSSProperties = { fontSize: 11.5, color: C.text2, marginBottom: 5, display: "block", fontWeight: 500 };
export const kStyle: CSSProperties = { fontSize: 10.5, fontWeight: 700, letterSpacing: "0.7px", textTransform: "uppercase", color: C.text3 };
